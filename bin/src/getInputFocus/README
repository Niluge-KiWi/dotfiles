== getInputFocus ==
Small C program to get the the name of the window that has the focus.

Used in emacs since from there we cannot know such information: 
frame-visible-p just knows if the frame is minimized or not.
